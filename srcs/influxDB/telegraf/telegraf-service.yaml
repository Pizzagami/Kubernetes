apiVersion: v1
kind: Service
metadata:
  name: telegraf
spec:
  type: ClusterIP
  ports:
  - name: statsd
    port: 8125
    protocol: UDP
    targetPort: 8125
  selector:
    app: telegraf
  externalName: tele.graf
