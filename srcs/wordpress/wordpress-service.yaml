apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
    app: wordpress
spec:
  ports:
  - name: http
    protocol: TCP
    port: 80
    nodePort: 5050
  selector:
    app: wordpress
    tier: frontend
  type: NodePort

---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: wp-pv-volume
  labels:
    type: local
    app: wordpress
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  capacity: 
    storage: 550Mi
  hostPath: 
    path: /data

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wp-pv-claim
  labels:
    app: wordpress
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 550Mi
